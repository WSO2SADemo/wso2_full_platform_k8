name: Integration - VIES Integration

on:
  push:
    paths:
      - 'apim-integration-artefacts-for-cicd-pipeline/integration/integrations-1/vies_integration/**'
      - '.github/workflows/vies-ci.yml'
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: self-hosted

    env:
      BALLERINA_NAMESPACE: ballerina 
      PROJECT_DIR: apim-integration-artefacts-for-cicd-pipeline/integration/integrations-1/vies-integration
      IMAGE_TAG: "1.0.0"
      DOCKER_REPO: "ramilu90/vies_integration" 
      # Config Map Name (Must match metadata.name in your local YAML file)
      CONFIG_MAP_NAME: "ballerina-values-vies-integration" 
      # Local Path to ConfigMap
      LOCAL_CONFIG_MAP_PATH: "/Users/ramindu/wso2/general_demo/is_demo_resources/k8-artefacts-apim-bi-elk/apim-integration-artefacts-for-cicd-pipeline/integration/integrations-1/vies-integration/config-map-vies.yaml"

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Print Ballerina Version
        run: bal version

      - name: Build Ballerina Project
        working-directory: ${{ env.PROJECT_DIR }}
        run: bal build
      
      - name: 🔐 Docker Login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          
      - name: Retag and Push Docker Image
        run: |
          BUILT_IMAGE=$(docker images --format "{{.Repository}}:{{.Tag}}" | grep "${{ env.DOCKER_REPO }}" | head -n 1)
          
          if [ -z "$BUILT_IMAGE" ]; then
            echo "Error: No image found matching '${{ env.DOCKER_REPO }}'"
            exit 1
          fi
          
          echo "Found built image: $BUILT_IMAGE"
          TARGET_IMAGE="${{ env.DOCKER_REPO }}:${{ env.IMAGE_TAG }}"
          docker tag "$BUILT_IMAGE" "$TARGET_IMAGE"
          docker push "$TARGET_IMAGE"

      - name: Ensure Namespace Exists
        run: kubectl create namespace ${{ env.BALLERINA_NAMESPACE }} --dry-run=client -o yaml | kubectl apply -f -

      # -----------------------------------------------------------
      # ✅ CHANGED: Apply Local ConfigMap File
      # -----------------------------------------------------------
      - name: Apply Local ConfigMap
        run: |
          echo "Applying ConfigMap from local path..."
          if [ -f "${{ env.LOCAL_CONFIG_MAP_PATH }}" ]; then
            kubectl apply -f "${{ env.LOCAL_CONFIG_MAP_PATH }}" -n ${{ env.BALLERINA_NAMESPACE }}
          else
            echo "❌ Error: File not found at ${{ env.LOCAL_CONFIG_MAP_PATH }}"
            exit 1
          fi

      - name: Ensure Ballerina Secret Exists
        run: |
          NAMESPACE="${{ env.BALLERINA_NAMESPACE }}"
          SECRET_NAME="ballerina-integration-secret"
          LOCAL_SECRET_PATH="/Users/ramindu/wso2/general_demo/is_demo_resources/k8-artefacts-apim-bi-elk/integration/ballerina-secrets.yaml"

          echo "Checking if secret '$SECRET_NAME' exists in namespace '$NAMESPACE'..."

          if kubectl get secret "$SECRET_NAME" -n "$NAMESPACE" > /dev/null 2>&1; then
            echo "✅ Secret '$SECRET_NAME' already exists. Skipping application."
          else
            echo "⚠️ Secret '$SECRET_NAME' NOT found."
            
            if [ -f "$LOCAL_SECRET_PATH" ]; then
              echo "Applying from local path: $LOCAL_SECRET_PATH"
              kubectl apply -f "$LOCAL_SECRET_PATH" -n "$NAMESPACE"
            else
              echo "❌ Error: Local secret file not found at $LOCAL_SECRET_PATH"
              exit 1
            fi
          fi
      
      - name: Deploy Kubernetes Resources
        run: |
          K8S_DIR="${{ env.PROJECT_DIR }}/target/kubernetes/vies_integration"
          echo "Applying resources from $K8S_DIR..."
          kubectl apply -f ${K8S_DIR} -n ${{ env.BALLERINA_NAMESPACE }}

      - name: Ensure Service & Patch Port 8080
        run: |
          NAMESPACE="${{ env.BALLERINA_NAMESPACE }}"
          SERVICE_NAME="vies-integration" 
          TARGET_PORT_NAME="vies-integration-port"
          
          echo "Current Deployments in $NAMESPACE:"
          kubectl get deployments -n $NAMESPACE
          
          # ✅ FIX: Search only for 'vies' to handle the 'integratio' typo
          DEPLOYMENT=$(kubectl get deployments -n $NAMESPACE -o name | grep "vies" | head -n 1 | sed 's|deployment.apps/||')
          
          if [ -z "$DEPLOYMENT" ]; then
            echo "❌ Error: Could not find any deployment matching 'vies'"
            exit 1
          fi
          
          echo "✅ Found Deployment: $DEPLOYMENT"
          
          # 1. Create Service if missing
          if ! kubectl get svc $SERVICE_NAME -n $NAMESPACE > /dev/null 2>&1; then
             echo "Service $SERVICE_NAME not found. Creating it for $DEPLOYMENT..."
             kubectl expose deployment $DEPLOYMENT -n $NAMESPACE --name=$SERVICE_NAME --port=8080 --target-port=8080 --type=ClusterIP
          fi
          
          # 2. Patch Port Name
          if kubectl get svc $SERVICE_NAME -n $NAMESPACE -o jsonpath='{.spec.ports[*].port}' | grep -q "\b8080\b"; then
             kubectl patch svc $SERVICE_NAME -n $NAMESPACE --type='strategic' -p='{"spec":{"ports":[{"port":8080,"name":"'"$TARGET_PORT_NAME"'"}]}}'
          else
             kubectl patch svc $SERVICE_NAME -n $NAMESPACE --type='json' -p='[{"op": "add", "path": "/spec/ports/-", "value": {"name": "'"$TARGET_PORT_NAME"'", "port": 8080, "targetPort": 8080, "protocol": "TCP"}}]'
          fi
          
          # 3. Export DEPLOYMENT name for the next step
          echo "DEPLOYMENT_NAME=$DEPLOYMENT" >> $GITHUB_ENV

      - name: Patch Deployment (Volume Mounts & Env)
        run: |
          NAMESPACE="${{ env.BALLERINA_NAMESPACE }}"
          # ✅ Use the auto-detected name from the previous step
          DEPLOYMENT="${{ env.DEPLOYMENT_NAME }}" 
          
          CONFIG_MAP_NAME="${{ env.CONFIG_MAP_NAME }}"
          
          # ⚠️ VERIFY: This must match the secret name established in "Ensure Ballerina Secret Exists"
          SECRET_NAME="ballerina-secret"

          echo "Patching Deployment: $DEPLOYMENT"
          echo "Using ConfigMap: $CONFIG_MAP_NAME"
          echo "Using Secret: $SECRET_NAME"

          CONTAINER_NAME=$(kubectl get deployment $DEPLOYMENT -n $NAMESPACE -o jsonpath='{.spec.template.spec.containers[0].name}')
          
          kubectl patch deployment $DEPLOYMENT -n $NAMESPACE --patch "
          spec:
            template:
              spec:
                volumes:
                - name: keystore-vol
                  secret:
                    secretName: $SECRET_NAME
                containers:
                - name: $CONTAINER_NAME
                  envFrom:
                  - configMapRef:
                      name: $CONFIG_MAP_NAME
                  volumeMounts:
                  - name: keystore-vol
                    mountPath: /home/ballerina/bre/security
                    readOnly: true
          "
          
          kubectl rollout restart deployment/$DEPLOYMENT -n $NAMESPACE
          kubectl rollout status deployment/$DEPLOYMENT -n $NAMESPACE --timeout=120s